"""Personal baseline (blueprint SS12) -- the "You vs. Yourself" reference
point: typical easy pace, HR, cadence, weekly volume, long-run distance,
workout intensity, performance range. Everything else (weather
intelligence, predictions, coach) compares against this baseline rather
than population norms.
"""


def compute_baseline(user_id) -> dict:
    raise NotImplementedError("Phase 3: aggregate recent easy-run activities into a baseline")
