# Pip-only extras that conda's environment.yml doesn't cover.
# Core scientific stack (pandas, numpy, scikit-learn, psycopg2, sqlalchemy,
# pytest) lives in environment.yml via conda-forge instead.
fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.2
alembic>=1.13
pandera>=0.19
xgboost>=2.0
lightgbm>=4.3
mlflow>=2.13
shap>=0.45
httpx>=0.27
python-dotenv>=1.0
pgvector>=0.2
garminconnect>=0.2.24
bcrypt>=4.1
pytz>=2024.1
